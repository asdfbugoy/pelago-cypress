import React from 'react';
import styled from 'styled-components';

const StyledText = styled.div`
    
`;

const Header = () => {
    return <header data-cy="header">
        
    </header>;
};

export default Header;